import { Query } from './friendAlarm.query';
import { Mutation } from './friendAlarm.mutation';
import { Subscription } from './friendAlarm.subscription';

export default {
  Query,
  Mutation,
  Subscription
};
