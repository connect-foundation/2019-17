type Chat {
  content: String!
  createAt: Idate!
  email: String!
  thumbnail: String
  nickname: String!
}

type ChatRoom {
  users: [User]!
  lastChat: Chat!
}

type ChatRooms {
  chatRooms: [ChatRoom]
}

type Mutation {
  createChatRoom(userEmail: String!, content: String!): [Chat]!
}
