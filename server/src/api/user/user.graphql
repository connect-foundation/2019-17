type User {
  email: String!
  nickname: String
  hometown: String
  residence: String
  thumbnail: String
}

type Query {
  findUserWithEmail(email: String!): User
  addUser(
    email: String!
    nickname: String
    hometown: String
    residence: String
    thumbnail: String
  ): User
}
