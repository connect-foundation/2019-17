scalar Upload

type User {
  nickname: String!
  residence: String!
  hometown: String!
  thumbnail: String
}

type Mutation {
  signUp(
    nickname: String!
    residence: String!
    hometown: String!
    googleId: String!
    file: Upload
  ): User!
}
