type User {
  nickname: String!
  residence: String!
  hometown: String!
  thumbnail: String
}

type Query {
  signup(
    nickname: String!
    residence: String!
    hometown: String!
    googleId: String!
    thumbnail: String
  ): User!
}
