type Friend {
  nickname: String
  thumbnail: String
  email: String!
  status: String!
}

type Query {
  loginUser(email: String): Boolean
  friends(email: String): [Friend!]!
}

type Subscription {
  updateUserState: Friend!
}
